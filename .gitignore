node_modules/
.env
.env.*
*.log
npm-debug.log*
.DS_Store
Thumbs.db
.idea/
.vscode/
*.suo
*.user

# Python venv for image/QR generation (recreatable — see .claude/skills/generate-3-top-decks-image/SKILL.md)
.venv-img/

# Generated price-comparison export output — CWD-relative default in
# fab-cli/src/commands/priceComparison.ts (`./price-comparison/`), so it can
# land at repo root when the CLI is invoked from there, not just inside
# fab-cli/. fab-cli/.gitignore also carries this pattern for invocations
# from inside fab-cli/.
price-comparison/

.claude/CHECKPOINT
.claude/ITERATIVE_UI_OFF
.claude/ui-hub/
.claude/gate-pass
.claude/telemetry.jsonl
.claude/lessons.jsonl
.claude/board-queue.jsonl
.claude/board-cache.json
.claude/neural-view/
.claude/merge-requirements.json

# Talishar card-implementation dossiers — ephemeral working state, never committed
.claude/talishar/dossiers/

# Corpus exporter output (pipeline/, SPEC-APP.md §7.1-§7.2) — exported chunk
# text and manifests are regenerable from the brains/rules KB/lore sources;
# never commit exported corpus content
pipeline/out/
# >>> spec-workflow managed
.claude/CHECKPOINT
.claude/ITERATIVE_UI_OFF
.claude/ui-hub/
.claude/gate-pass
.claude/telemetry.jsonl
.claude/lessons.jsonl
.claude/board-queue.jsonl
.claude/board-cache.json
.claude/board-comments-seen.json
.claude/neural-view/
.claude/merge-requirements.json
.claude/.flush*
.claude/worktrees/
.claude/identities/*/brain/index.sqlite3
.claude/identities/*/brain/.staleness-cache.json
.claude/assistant/
# <<< spec-workflow managed
