# Per-run manifest.json/state.json/config.json ARE committed (SPEC-APP.md
# §8.1's "committed training manifest per run") — only the pulled LoRA
# adapter weights themselves stay out of git.
*/adapters/
